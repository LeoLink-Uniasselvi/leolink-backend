# ============================================
# DEVELOPMENT ENVIRONMENT (LOCAL)
# ============================================
# Copie este arquivo para .env para desenvolvimento local
# Para produção, use .env.production com credenciais do Render

# Database Configuration (PostgreSQL Local via Docker)
# Para desenvolvimento local, use docker-compose up -d
DB_HOST=localhost
DB_PORT=5432
DB_USER=leolink_user
DB_PASS=leolink_password
DB_NAME=leolink

# Application Port (Render usa PORT automaticamente em produção)
APP_PORT=5000

# JWT Secret (MUDE ISSO EM PRODUÇÃO!)
# Gere um novo: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET='your-super-secret-jwt-key-change-this-in-production'

# CORS Origins (separado por vírgulas para múltiplas origens)
# Desenvolvimento: http://localhost:3000
# Produção: https://seu-frontend.onrender.com
CORS_ORIGIN=http://localhost:3000

# Environment settings
NODE_ENV=development

# Database logging (set to 'true' only for debugging queries)
DB_LOGGING=false